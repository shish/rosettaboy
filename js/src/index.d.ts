type u8 = number;
type u16 = number;
type i8 = number;

type SDL_Point = { x: number; y: number };
type SDL_Rect = { x: number; y: number; w: number; h: number };
type SDL_Color = { r: u8; g: u8; b: u8; a: u8 };
